{
	"$schema": "../gen/schemas/desktop-schema.json",
	"identifier": "trusted-app-windows-development",
	"description": "The native API the development Dictation window reaches: unrestricted HTTP(S) egress through the Tauri HTTP plugin, and the recording and transcription operations the public @tironian/app client exposes (ADR-0186). Not a sandbox, because the window already runs as Tironian on the shared origin; this file is API wiring, so it names product operations and never model administration or device enumeration.",
	"local": false,
	"windows": ["dictation"],
	"remote": {
		"urls": ["http://127.0.0.1:*"]
	},
	"permissions": [
		{
			"identifier": "http:default",
			"allow": [
				{ "url": "http://*" },
				{ "url": "https://*" },
				{ "url": "http://*:*" },
				{ "url": "https://*:*" }
			]
		},
		"allow-start-recording",
		"allow-stop-recording",
		"allow-cancel-recording",
		"allow-current-recording",
		"allow-transcribe-recording",
		"allow-prewarm-model",
		"allow-get-local-transcription-readiness",
		"core:event:allow-listen",
		"core:event:allow-unlisten"
	]
}
