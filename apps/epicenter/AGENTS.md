# Epicenter

One desktop application host. Bun serves the trusted SPAs, APIs, WebSockets,
and Home session; Rust owns native application mechanisms.

Design authority: ADR-0190 (a compiled application is a declared `dist/<id>` build; its "`epicenter-host` says the host owns its replica" half is withdrawn by ADR-0226, which a build no longer declares at all), ADR-0209 (Epicenter is an application beside the others: it shows the data raw, the applications are the crafted views, and the OS is the launcher and switcher), ADR-0189 (one launchable list, one Home-owned launch verb, per-application windows; its Home-as-shell framing is withdrawn by ADR-0209), ADR-0179 (one static-artifact admission boundary for one immutable app catalog; supersedes ADR-0153), ADR-0180 (one host-owned active local transcription model), ADR-0152 (what Home owns; read its title as history, since ADR-0209 withdrew "above"), ADR-0151 (owner-scoped local stores), ADR-0118 (one trusted application origin), ADR-0080 (remote devices attach to the session, never to per-app endpoints), and ADR-0113 (Home owns command semantics; transports only frame and deliver them).

## Shape

- `src/applications.ts` declares the compiled applications and is what Home lists as launchable, in one `{ id, title }` shape served at `/api/apps`. There is no admitted catalog: a single-app product admits no other apps, and `COMPILED_APPLICATIONS` is the whole list. Do not reintroduce a second, discovered source of application IDs.
- Epicenter is an application beside the others, not a shell above them (ADR-0209). Applications keep their own windows and the OS is the launcher and switcher, so do not build an in-app application switcher, a command palette over rows, or anything that indexes rows into an OS search index. Closing Epicenter must leave every other window running. The raw-view pane ADR-0209 described is **deleted**, not broken: it opened a replica the host owned, and ADR-0226 withdrew that. Home has three panes now, Apps, Chat and Settings. Rebuilding a raw view is not a reimport of `src/inspect.ts`; an application on the store writes to its own client-owned store, so a reader that wants an application's rows has to become a replica of the authority that application uses, and that is a design nobody has written down yet.
- Home renders one row and calls one native verb (`launch_application`), which dispatches to the one compiled built-in app's window (Whispering) and settles on that window's real outcome rather than on having scheduled it. Rust resolves the ID against its own compiled app table; an ID naming anything else is refused rather than opened. Do not add a second launch command, do not reintroduce an `app-` window class, and do not reuse ADR-0181's app-facing `openApp(appId)` name for this verb.
- A compiled application is exactly a `dist/<id>` build `COMPILED_APPLICATIONS` declares. `loadStaticAssets` loads every one and the server serves each below `/apps/<id>/` through the shared contained resolver, so adding one is a list entry plus a build script, never another asset field, resolver, or route handler. A declared application that did not build refuses the boot (ADR-0190). Its document is held in memory, gated behind a browser session, stamped with the auth bootstrap, and hashed into the one CSP. Serving an app's document straight off disk skips all four, and the app boots blank because the browser refuses its own start script.
- There is no host-owned replica, and a build cannot reach one. Every build opens its own store and reaches one authority per signed-in account (ADR-0226, widened to every app window by ADR-0227). The `epicenter-host` resolve condition survives for exactly one thing: the host really does broker a credential its windows cannot obtain, so `#platform/auth` and `#platform/instance` select the desktop bootstrap under it. Never detect the host at runtime, never widen `tauri` to mean it, and never reintroduce a storage seam behind it.
- An application window gets a capability file only when it calls a native command. Same-origin HTTP to the Bun host needs no grant; the trusted-app capability is Whispering's own authority, not a compiled application's default (ADR-0190).
- `src/main.ts` resolves the one Epicenter data root by calling `epicenterDataRoot()` from `@epicenter/constants/app-data`, and derives `blobs/` below it. There is no `data/`: the store that lived there was the host-owned plane ADR-0226 refused. Rust does not pass a root in, and no other TypeScript may compute one: a host and a CLI that disagree here write to two different mailboxes (ADR-0201). The one native resolution left, `src-tauri/src/app_data.rs`, exists because the staged-recording blob store runs before the host could have told it anything, and it is pinned equal to the TypeScript one by a test.
- Every trusted app owns one directory under that root, `apps/<app-id>`, named by the id it is admitted or composed under (ADR-0201). Allocation is nominal: the host issues the id and creates nothing, and it reclaims nothing when an app leaves. There is no reserved-id list and adding one would be a regression: an admitted app's id is the id it declares, which always contains a dot, and every id this host issues itself is a bare label, so the two sets cannot collide by grammar (ADR-0210). Do not give a webview a path into any of it: reach is undecided, and the handle has no filesystem in it (ADR-0181).
- **The host-owned data plane is deleted** (ADR-0226, ADR-0227). Gone with it: the desktop store owner, the `honeycrispMirrorLens` that re-declared another application.s workspace id, the `src/folder/` markdown renderer and its SQLite projection (ADR-0207, ADR-0208), `src/inspect.ts` and the `/api/home/inspect` routes, the `/api/data` proxy and its observation socket, and the durable Home conversation rows. Do not rebuild any of them here. A host serves bundles and brokers credentials; a markdown mirror or a raw view belongs to whoever owns the authority those rows live in.
- `src/host.ts` composes optional boxed stdio MCP tools and optional read-only local sources into one workspace idd `ToolCatalog`. It opens no store and owns no data directory. Its conversation transcript is in memory for the session: the host holding a durable transcript needs a document of its own, and nobody has decided it should have one.
- Local sources remain host-owned and are never remote routes or capabilities.
- Command semantics belong to the host session, not the WebSocket adapter. Chat sends, direct invocations, approval answers, and later palette or voice commands must route through one host-owned session command API.

## Refusals (do not reopen without a new ADR)

- No daemon `mount.ts` as the composition model; the mount path is CLI/projection for one app.
- No MCP for first-party in-process apps; MCP stays the boxed-app airlock (ADR-0081).
- No loose in-process TypeScript tool modules in v1; future scripting starts from an out-of-process runner unless a new ADR explicitly accepts unsafe developer-mode host imports.
- No bundled Tauri SPA plus side IPC; Bun serves the SPA and the API from one loopback origin.
- Epicenter admits inert built app folders and never installs dependencies, runs an app's build system, or reads application source. A folder from a publisher URL, a self-hosted builder, a local developer build, or offline media enters through the same boundary with the same authority (ADR-0179).
- No registry, publisher identity, artifact signing, or update mechanism. ADR-0179 decides admission only; anything downstream of it needs its own ADR.
- No per-app permission, capability grant, or device prompt. An app window runs as Epicenter: shared origin and session, OS-granted browser device access, and the one app-window native API. Do not describe the capability file as a sandbox (ADR-0179).
- Applications never choose the local transcription model. Home owns the one active model and its administration; apps request transcription and may still choose their transcription route (ADR-0180).
- No new Home pane, no global rail, and no installation, update, or removal UI. Home has Apps, Chat, and Settings. A rail still needs a launchable list large enough to earn it, and app management still needs real operations before it needs a screen (ADR-0189). Re-adding a Data pane means first deciding how a reader becomes a replica of somebody else's authority, which is an ADR, not a component.
- The loopback server always binds `127.0.0.1` and rejects every request without the per-launch token; this ships with the first server version, not later (ADR-0084).
- No HTTP command route, Tauri IPC command path, stdio command protocol for the browser UI, generic synced command table, or transport-adapter framework until a real second consumer earns it. The current WebSocket is a session adapter, not the architecture.
