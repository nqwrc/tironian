# status

state: active
remote: github-public
updated: 2026-09-14
stale-after-days: 30

## kpi
| kpi | target | current | as-of |
|---|---|---|---|

## focus
- Tironian, a local-first dictation app for macOS and Windows, public at nqwrc/tironian. No upstream name remains in tracked files or paths outside the license allowlist: `bun run check:brand-residue` reports 0 (from 15216) and runs inside `check:structure`. What remains are the copyright lines, license texts and modified-version notices that AGPL-3.0 and MIT require.
- Scope: the dictation window (`apps/tironian`), the only window, served by the Tauri host (`apps/desktop`). Removed: sign-in, accounts, sync, hosted inference, the web deployment, the sibling notes app, the app catalog, the Home chat pane, and every app, package and document the product did not import. Identity: bundle `app.tironian`, loopback ports 41730/41731, scheme `tironian://`, sidecar `tironian-host`, crate `tironian`. Local transcription and bring-your-own-key providers remain.
- Verified at 4735c9804: desktop run 34613692147, macOS smoke 4/4 (dictation window "Tironian" on screen at launch), dictation suite 319/0, `cargo test --release` 129/0, host TypeScript suite 41/0; Windows build and installer audit green. Local Windows: typecheck 0 errors, all checks pass, `cargo test` 118, package tests 12 failures, all Windows-only and present in the baseline.

## next
- Nicola's decisions: Apple Developer ID and Windows code-signing purchases; accent `#D97757`; KPIs for this repo.
- Merged 2026-09-13: nqwrc/tironian#3 (38d7ea2ac) and nqwrc/tironian#4 (4193756b8), CI green on macOS, Windows and quality for both.
- `feature/single-window` (ADR-0245): the Home window is gone. Model settings live in the dictation app under Settings, Privacy & Processing; the dictation window holds the model administration grants, the tray has no Model settings item, `tironian://app/home` is refused, and `font-src data:` is removed (the dictation build serves 28 woff2 files, 0 `data:` fonts). Checks: `cargo test` 116/0, dictation suite 320/0, host 37 pass with 2 environmental failures (Windows SIGTERM exit 143 as on main; compiled-host test cannot bind 41730 while the installed app runs). Rendered in the real Tironian Dev WebView over CDP: Settings, Privacy & Processing lists the three models with live download state and the unload policy, fonts load without `data:`, and the console holds only the Ctrl+Alt+Space conflict. The model store reads the host when the panel mounts, not at import. Installed locally 2026-09-13 (installer exit 0); the installed app's Settings shows Whisper Small active. PR: nqwrc/tironian#3 (merged).
- Vivavoce design, deliverable 4 (project "App redesign directions" on Claude Design, 10 screens): deliverable 2 tokens already ship in `brand.css`. Merged into main via nqwrc/tironian#5 (b28f1c0c9, 2026-09-14): the labeled sidebar (4b shell), open by default at 13rem, Settings pinned at the bottom with the theme toggle, and a stats footer (wpm dictated and median wait from the newest 50 completed recordings, plus the local or cloud route; the design's "% local" is not computable because rows do not record the service). Seen on the installed app over CDP (2026-09-13): 88 wpm dictated, 0.8s median wait, cloud transcription (Groq). The stats math is unit-tested. Also done: Home 4b, the mark in a circle that starts and stops manual recording, the shortcut chip, and a last-transcription card with Copy and Open; the capture-mode switch and pipeline selectors stay on Home, smaller, until 4d. Seen on the installed app: "Ready to listen", the Ctrl+Win chip, and the last card. Also done: Recordings 4c, rows grouped by day with time, one line of text and duration, copy, transcribe and delete on hover, the detail modal on click, search in text, the count and time dictated in the header, and bulk transcribe, copy and delete behind Select (Nicola's choice); the column picker and pagination are gone, and play lives in the detail modal. Also done: the Settings header and tabs of 4d, underlined in `--voce`, Shortcuts first and Capture renamed Recording; five groups stay, because the design's Dictation tab duplicates the Dictation page and sounds live under Recording. Also done: Dictation 4f, whose three groups (Polish, voice commands, dictionary) already existed, now stacked under hairlines with mono labels instead of cards. Also done: Recipes 4g and Snippets 4h as plain lists under one heading with the New button beside it, built-ins marked and without actions, edit and delete on hover, snippet triggers quoted in sans and delivered text in mono. All five pages seen on the installed app over CDP (2026-09-13, installer exit 0): 84 recordings, 9m dictated, 91 wpm, 0.6s. The duplicate "tironian" bar above config pages now shows only below 768px, and the settings tab strip hides its scrollbar. Also done: detail modal 4i (mono meta line, delivered text read-only with Copy, footer Delete, Close, Save) and shortcuts 4j (one searchable list, the scope as a word beside each key). Also done (a47a82ea0, Nicola: "simplify them so I don't have to scroll"): each settings tab reads on one screen. Recording has App, Capture and Output; recipe output, pill position and bitrate sit under "More options"; eight sound switches became one with the per-cue list behind "Choose sounds"; Privacy & Processing shows one provider's setup at a time through a Provider select instead of every provider card, with the Speaches guide folded; browsing never switches the route, which changes only through a "Use" button enabled once that provider is configured (fix after review: the first cut switched on select and could point capture at a keyless provider); selects render as 4d rows; App rules is a plain list like Recipes; the #recording and #sounds anchors the old routes redirect to are kept; no setting key removed. Provider base URL overrides and the custom endpoint's optional key sit under Advanced, the text provider select is a row, and Data shows one row per action with the export checklist behind "Choose what to include" (00a57fffb). Suite 336/0, typecheck 0. Installed 2026-09-13 18:24 (installer exit 0) and measured over CDP at 1120x760, overflow in px: Recording 2, App rules 0, Data 0 (was 438), Shortcuts 0, Privacy & Processing 231 (was 601; the password-field section sits below the fold). Browsing OpenAI with no key shows "Use OpenAI" disabled and "Finish the setup below", and the route still reads "Audio is sent to Groq."; console clean. Also done: pill states 4a (fd634e345), replacing the Wispr Flow capsule the pill had been built to: idle is a flat line that dims to 60% after 4s, listening is five coral bars on the real mic level (dim until VAD latches speech), processing is P1 taken literally, a musical note (♪, drawn as SVG) passing along the line instead of the artboard's waveform (Nicola's choice 2026-09-13, over the U+204A mark first shipped), delivered is D1, a green check held 1.2s (DELIVERED_FLASH_MS 900 to 1200); stop, cancel and ship-raw show on hover; withheld, failed and the amber clipboard reach keep their words. Idle is drawn, so the overlay shows on a null status and ignores cursor events at rest (`core:window:allow-set-ignore-cursor-events` on the dictation window). Installed 2026-09-13 19:05 (installer exit 0); every state driven over CDP by emitting the status and mic-level events from the main window and screenshotting the overlay webview: idle 52x30 at 0.6, listening 57x34, hover 111x34, processing 56x34, delivered 50x34, clipboard 116x34, withheld 214x34, both consoles clean. This machine has reduced motion on (matchMedia true), which holds the note still at the line's centre; the idle dim still lands at 4s in one step. Reinstalled 2026-09-13 19:31 (installer exit 0, 04d91cd9f): opacity 1 at 0.3, 1.5 and 3.5s after a return to rest, 0.6 at 4.8s (animation finished at 4000 ms); the note renders with its three parts; consoles clean. The processing note now passes alone, without the line under it (7cb3db332, Nicola). Also done: onboarding 4e (9b61073dd), three light steps over the app on first run, gated by the device flag `onboarding.completed`: the U+204A mark and the name in Newsreader Italic (`@fontsource/newsreader` 5.3.0, loaded only there), the microphone step through `tauri.permissions.microphone`, and a try step with the live push-to-talk chord, the real pill and the words produced; copy is true to the setup (on-device versus the named provider; recordings kept, not discarded), and an unready transcription route sends the try step to Privacy & Processing. Installed 2026-09-13 20:15 (installer exit 0) and checked over CDP: flag null at launch, Newsreader italic loaded, the try step reads "Hold Ctrl+Win and say something." with the pill at rest and the placeholder, and finishing set the flag to true and removed the overlay; consoles clean. Nicola walked steps 1 and 2 himself during the run, so only step 3 was screenshotted; the script then cleared the flag, so the onboarding shows once more on the next launch. Every Vivavoce deliverable-4 screen is now implemented. PR: nqwrc/tironian#5 (opened 2026-09-13, 25 commits ahead of main, 0 behind); CI green on 977b3f6d2: quality 2m20s, macos-aarch64 10m37s, windows-x86_64 25m3s, GitGuardian pass. Merged 2026-09-14 as b28f1c0c9 on Nicola's word, all four checks green on the head 0193d35fa. Not carried over, per docs/brand/tironian.md: the vivavoce wordmark, the soundwave mark, Italian as source language.
- `feature/ctrl-win-push-to-talk` (ADR-0246, stacked on #3): on Windows, push-to-talk defaults to a Ctrl+Win hold detected by a low-level keyboard hook that never swallows keys, masks Start with `0xE8`, and releases on any third key; macOS and Linux keep their chords, and a stored chord outranks the new default. `cargo test` 127/0, dictation suite 326/0, typecheck 0 errors. Installed locally 2026-09-13; this device's stored Ctrl+Alt+Shift+F12 was replaced with Ctrl+Win through the Settings recorder; two physical holds recorded 1890 ms and 2140 ms, each stopped on release and transcribed (Tironian.log 14:06Z, 14:07Z); Start stayed closed on release (Nicola). The AutoHotkey `superwhisper_hotkey.ahk` is retired: its Startup shortcut sits in `Startup-disabled`. PR: nqwrc/tironian#4 (merged).
- Follow-ups, not defects: ADR-number citations in comments name design records that no longer ship; the `packages/sqlite` browser adapter has no consumer; seven sound files have no recorded provenance; no generated third-party notices file ships with the installers.

## blockers
- D: has 0.7 GB free of 64 GB (2026-09-13); the largest folders are `D:\_cache` 19.1 GB and the cargo target `D:\ct` 15.8 GB. Builds on D: fail with os error 112, so this session builds into `C:\ct`. Freeing space is Nicola's call.
- The weekly `DevDriveSnapshot` task (Sundays 18:00, `D:\.dotfiles\machines\windows\devdrive-snapshot.ps1`) dismounts D: for the whole copy of `Developer.vhdx` (63.4 GB on 2026-09-13), so every running shell, dev server and build on D: loses its files mid-flight; it remounted in `finally` within about 90 s. Recommendation: move the trigger to a night hour, or skip when a process holds files on D:.
- Tironian Dev and the installed app share the data root `%APPDATA%\app.tironian`, so a dev launch sweeps production's `.staging/rust` (seen 2026-09-13, nothing was in flight). Do not run the two side by side while dictating until the sweep or the root is scoped.
