# See https://help.github.com/articles/ignoring-files/ for more about ignoring files.

# Dependencies
node_modules
.pnp
.pnp.js

# Testing
/coverage

# Production
build
dist

# Misc
.DS_Store
*.pem
tsconfig.tsbuildinfo

# Debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*
*.log

# Agent / benchmark scratch (machine-local, never commit)
bench-oneshot.ts
20??-??-??-*.txt

# ENV files
.env
.env.*
!.env.example
!.env*.example
.dev.vars
apps/local-books/.infisical.json

# Vercel
.vercel

examples/**/matter.sqlite

# WXT (browser extension framework)
.wxt
.output
.svelte-kit

# Wrangler (Cloudflare Workers local state)
.wrangler
.conductor/

# Reddit exports (personal data)
reddit_export.zip

# Temporary search clones (development only)
tmp-search/
.tmp/
tmp-yjs/

# y-sweet server data (created when running the sync server locally)
y-sweet-data/

# Editor / agent tooling state (machine-local)
.opencode/
.sisyphus/
.claude/worktrees/
.claude/scheduled_tasks.lock

# jsrepo build output (regenerated by `bun run jsrepo:build`)
registry.json
test-results/

# Smoke-test and tooling run logs (machine-local, regenerated per run)
.artifacts/

# Sentry native crash-handler run state (created by the Tauri host at runtime)
.sentry-native/
